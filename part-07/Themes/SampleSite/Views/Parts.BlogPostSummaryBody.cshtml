@* This template is used to override the rendering of the body content for a blog
   post when in summary form. Rather than rendering an excerpt, this version renders
   the entire content of the blog post *@

@Model.Html
